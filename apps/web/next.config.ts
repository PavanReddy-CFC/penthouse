import type { NextConfig } from 'next';

// No environment-specific values here. All configuration comes from
// environment variables (see env/web.env.example).
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
